git init
git add .
git commit - m "Message"
git status
git log (hit q to exit)
git checkout <unique-commit-id>

GIT REMOTE COMMANDS

git remote add origin <url>
git remote -v (to check )
git push origin master
git pull origin master